%   3/13 開發日誌
#   main.js主要是增加了對不同類型的客戶端的支援，不同類型客戶將會有不同的方法庫，並且單一客戶如果連結了之後再傳就直接進入方法庫，
    並不需要再identify，他的邏輯是一開始客戶端握手的時候就會傳送identify，並且由js接收並解析，如果是python他將會建立一個只屬於這
    一個python的伺服器，並且開啟n個監聽器，專門面對這一個python，所以如果有x個python客戶端，他將會有x*n個的監聽器
#   main.py目前還是屬於測試程式，之後開發到一個地步(應該是在superglue那邊)需要將他寫的更general更有邏輯，目前他是每三秒要求讀取
    json檔，並會返回座標情況
    ，如果正常則會繼續進行，如果沒有座標就會觸發"方法論"，向js發送 "要求cesium圖"的指令，目前是寫到這樣
-- 總結:1. main.js增加了對不同客戶端的支持 2.main.py每三秒傳要求讀取json並返回座標狀況如果錯誤，觸發方法論。下次寫會從js伺服器的"
   接受no_coodinate"開始寫，main.js已經有了基礎架構，主要邏輯應該是不會動了，main.py還需要進一步整理(預計在superglue那邊)

-- 啟動伺服器 http-server -p 5500 啟動瀏覽器 http://localhost:5500/main.html

4/22
-- 目前的問題是我可以先模擬一個樹梅派數據他的檔名是test.json之後只要和另一個分支merge起來就可以獲得來自樹梅派的數據，現在的問題主要是在
json handler的問題，因為json_handler是判斷test.json是否有座標訊息來判斷是否要建立子資料夾，也就是a、b、c資料夾，但是照理來講即使沒有也
要建立資料夾，判斷是否有座標資訊並不會影響是否要建資料夾，而是決定是否要傳送給python他沒有座標資訊，另外就是，如果是連續時間的定位，我需要
的是在前一個serial_number獲得他的飛行數據，所以總結來說我需要改有兩點第一個是確定讀取座標邏輯正確，另一個是將讀取flight_information改為上
一個時間節點，然後還有python最後需要把數據放回a資料夾，原本可能是null，要改為計算出來的。